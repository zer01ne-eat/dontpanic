import React from 'react';
import { HomeContent, HomeTitle } from './styles';

const Home = () => {
    return (
        <HomeContent>
            <HomeTitle>Home</HomeTitle>
        </HomeContent>
  );
};

export default Home;
